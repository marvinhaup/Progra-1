#include <stdio.h>
#include <stdlib.h>

int main()
{
    float preisangabe;
    scanf("%f", &preisangabe);
    printf("der von ihnen eingegebene preis beträgt %f euro", preisangabe);

    return 0;
}
